/* GitHub Pages stays static; run.py replaces this response in local-server mode. */
window.IELTS_LAB_CONFIG = Object.freeze({
  deployment: "static",
  serverFeatures: false,
  onlineGenerationAvailable: false,
  privacy: "Core practice stays in this browser. Online generation is unavailable in the static demo.",
});
